
// outsource dependencies
import { fork } from 'redux-saga/effects';

// local dependencies
/**
 * common root watcher
 *
 * @public
 */
export default function * rootWatcher () {
    // yield fork();
}
